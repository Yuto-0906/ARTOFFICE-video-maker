# ARTOFFICE広報動画作成ソフト

バンドサークルで撮影した複数のライブ動画を，出演順に1本へまとめるWindows用アプリです。

動画の前後カット，バンド名表示，映像のつなぎ合わせ，YouTube用チャプター，集合写真からのサムネイル作成を，画面の順番どおりに進めるだけで行えます。

## できること

- 10～30組ほどのライブ動画を出演順に並べて結合
- 1バンド分が複数ファイルに分かれた動画を直結
- 各動画の不要な冒頭・末尾をカット
- 各バンドの開始時にバンド名を左下へ表示
- バンド間を自然なクロスフェードで接続
- 1080pまたは1440pの完成動画を作成
- YouTube概要欄へ貼り付けるチャプターテキストを作成
- 集合写真を16:9に切り抜いてサムネイルを作成
- 編集途中の内容をプロジェクトとして保存

## 対応しているパソコン

- Windows 10またはWindows 11
- 64bit版

アプリの利用に必要なフォントや動画処理用ファイルは同梱されています。別途インストールする必要はありません。

macOSには対応していません。

## ダウンロードと初回起動

このアプリはインストーラーを使いません。ZIPをダウンロードして展開するだけで利用できます。

1. [最新版のダウンロードページ](https://github.com/Yuto-0906/ARTOFFICE-video-maker/releases/latest)を開きます。
2. ページ下部の「Assets」を開き，`ARTOFFICE-video-maker-v0.2.0-windows-x64.zip`をクリックします。
3. ダウンロードしたZIPを右クリックし，「すべて展開」を選びます。
4. 展開先を選び，「展開」を押します。デスクトップやドキュメント内で構いません。
5. 展開してできたフォルダーを開きます。
6. `ARTOFFICE-video-maker.exe`をダブルクリックします。

> ZIPの中にあるアプリを直接起動しないでください。必ず「すべて展開」を行ってから起動してください。

### 「WindowsによってPCが保護されました」と表示された場合

初回起動時にWindows SmartScreenの警告が表示されることがあります。

1. このGitHubリポジトリのReleaseからダウンロードしたファイルであることを確認します。
2. 警告画面の「詳細情報」をクリックします。
3. 表示された「実行」をクリックします。

### アプリのフォルダーについて

`ARTOFFICE-video-maker.exe`だけを別の場所へ移動しないでください。同じフォルダー内の`tools`や`resources`も必要です。移動するときは，展開したフォルダーごと移動してください。

## 動画を準備する

基本的には1バンドにつき1本のMP4を用意します。カメラの都合で1バンドの動画が2本以上に分かれていても，読み込み後に連結できます。

ファイル名は次の形にしてください。

```text
出演順_バンド名.MP4
```

例：

```text
1_MOSHIMO.MP4
2_Mrs. GREEN APPLE.MP4
3_軽音バンド.MP4
```

- 出演順は半角数字にします。
- 数字とバンド名の間には半角の`_`を1つ入れます。
- 日本語，英字，空白を含むバンド名を使用できます。
- 分割されていない動画では，同じ出演順を重複させないでください。
- 元動画はアプリによって変更されません。

同じライブの動画を1つのフォルダーへまとめておくと，読み込みが簡単です。

## 基本的な使い方

画面上部にある次の4つの手順を，左から順に進めます。

### 1．動画読込

1. 「イベント名」にライブ名を入力します。ここで入力した名前が完成ファイル名になります。
2. 動画をまとめた場合は「フォルダーを選択」，個別に選ぶ場合は「MP4を選択」を押します。
3. 読み込まれた動画の順番とバンド名を確認します。
4. バンド名が違う場合は，アプリ上で書き直します。元のMP4ファイル名は変更されません。
5. 順番を変える場合は，行の左端にある「⠿」をドラッグします。うまくドラッグできない場合は，右端の「↑」「↓」でも移動できます。

#### 1バンドの動画が複数に分かれている場合

1. 分割された動画をすべて読み込みます。
2. 「⠿」または「↑」「↓」を使い，パート2をパート1のすぐ下へ移動します。
3. パート2の行にある「前と連結」を押します。
4. 表示が「✓ 前と連結」になったことを確認します。
5. 3本以上に分かれている場合は，パート3以降にも同じ操作を行います。

連結した動画は同じバンドとして扱われます。分割部分にはクロスフェードを入れず，前の動画の直後へそのまま接続します。バンド名表示とYouTubeチャプターは最初のパートにだけ作成されます。並べ替えるときは，連結したパートがバンド単位で一緒に移動します。

「書き出し時に変換します」と表示されてもエラーではありません。そのまま書き出せます。

### 2．トリミング

1. 左側の一覧から編集するバンドを選びます。
2. 動画を再生して，残したい開始位置を探します。
3. 開始点側の「現在位置を設定」を押します。
4. 残したい終了位置を探し，終了点側の「現在位置を設定」を押します。
5. 必要に応じて範囲ハンドル，時刻入力，前後1フレーム移動で微調整します。
6. 「開始へ」「終了前を確認」で，カット位置を確認します。
7. 別バンドへの切り替わりは「0.5秒クロスフェードを確認」，分割動画の境目は「連結部分を確認」で確認します。

同じ操作をすべてのバンドに行います。

### 3．サムネイル

サムネイルが不要な場合は，何も選ばず次へ進めます。

1. 「写真を選択」を押し，集合写真を選びます。
2. 写真をドラッグして位置を調整します。
3. 拡大率のスライダーを動かし，全員が16:9の枠内に入るよう調整します。

JPG，PNG，HEIC，HEIF形式の画像を使用できます。写真へ文字は追加されません。

### 4．書き出し

1. 「1080p」または「1440p」を選びます。迷った場合は1080pで構いません。
2. 「選択」を押し，完成ファイルを保存するフォルダーを選びます。
3. 表示されたファイル名とチャプター内容を確認します。
4. 「書き出しを開始」を押します。
5. 完了表示が出るまでアプリを閉じないでください。

長いライブでは書き出しに時間がかかります。ノートパソコンは電源へ接続して利用することをおすすめします。

## 作成されるファイル

保存先には次のファイルが作られます。

- `イベント名.mp4`：YouTubeへアップロードする完成動画
- `イベント名_チャプター.txt`：YouTube概要欄へ貼り付ける文章
- `イベント名_サムネイル.jpg`：サムネイルを設定した場合のみ作成

同じ名前のファイルが既にある場合は，上書きしてよいか確認されます。

## YouTubeへチャプターを設定する

1. 作成された`イベント名_チャプター.txt`を開きます。
2. 中身をすべて選択してコピーします。
3. YouTube Studioで完成動画の詳細画面を開きます。
4. コピーした内容を概要欄へ貼り付けます。
5. 保存します。

チャプターは次のような形で作成されます。

```text
0:00:00 MOSHIMO
0:20:21 Mrs. GREEN APPLE
```

YouTubeでは，チャプターが3件未満の場合や，各チャプターが10秒未満の場合に，自動認識されないことがあります。アプリに警告が表示されても，テキストファイル自体は作成されます。

## 編集途中で保存する

上部の「保存」または「名前を付けて保存」を押すと，編集内容が`.aovproj`ファイルとして保存されます。

- 自動保存ではありません。作業の区切りごとに保存してください。
- 次回は上部の「開く」から`.aovproj`を選ぶと，編集を再開できます。
- アプリを閉じるときに未保存の変更がある場合は，「保存して終了」「破棄して終了」「キャンセル」から選べます。

プロジェクトファイルの中に動画本体は保存されません。編集が終わるまで，元のMP4を移動・削除・名前変更しないでください。

## アプリを更新する

1. アプリを終了します。
2. [最新版のダウンロードページ](https://github.com/Yuto-0906/ARTOFFICE-video-maker/releases/latest)から新しいZIPをダウンロードします。
3. 古いフォルダーとは別の場所へZIPを展開します。
4. 新しいフォルダーの`ARTOFFICE-video-maker.exe`を起動します。
5. 必要なプロジェクトが開けることを確認してから，古いアプリのフォルダーを削除します。

元動画，完成動画，`.aovproj`は，アプリのフォルダー外に保存していれば削除されません。

## アプリを削除する

アプリを終了し，展開したアプリのフォルダーを削除してください。アンインストール操作は必要ありません。

## 困ったとき

| 症状 | 確認すること |
|---|---|
| ZIPを開いた状態では起動しない | ZIPを右クリックして「すべて展開」してから起動してください。 |
| SmartScreenの警告が表示される | Releaseから取得したことを確認し，「詳細情報」→「実行」を選びます。 |
| 画面が開かない，真っ白になる | Windows Updateを実行してください。改善しない場合は[Microsoft WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/)を導入してください。 |
| 動画の順番がおかしい | ファイル名の先頭が半角数字になっているか，同じ番号が重複していないか確認します。 |
| ドラッグすると禁止マークが出る | 行全体ではなく，左端の「⠿」をドラッグしてください。「↑」「↓」でも並べ替えられます。 |
| 分割動画の間にクロスフェードが入る | 後ろの動画を前の動画の直下へ移動し，「前と連結」を押してください。 |
| 元動画が見つからないと表示される | MP4を元の場所へ戻すか，アプリの再リンク機能で保存先フォルダーを指定します。 |
| 書き出しが始まらない | 保存先の空き容量と，元動画が他のアプリで使用中になっていないか確認します。 |
| YouTubeでチャプターが表示されない | 3件以上あるか，各チャプターが10秒以上あるか，先頭が`0:00:00`か確認します。 |
| エラー画面が表示された | エラー内容をコピーまたは撮影し，操作手順と一緒にGitHubのIssuesで知らせてください。 |

## 現在対応していないこと

- 複数カメラの切り替え編集
- 自動トリミング
- 音量調整，色補正，字幕追加
- サムネイルへの文字入れ
- YouTubeへの直接アップロード
- macOS対応
- 自動保存，自動更新

第三者ソフトウェアに関する表示は[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)を参照してください。
